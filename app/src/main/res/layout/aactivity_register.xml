<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white">

    <!-- Toolbar con los mismos parámetros exactos pero mejorado visualmente -->
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar2"
        android:layout_width="425dp"
        android:layout_height="63dp"
        android:background="@color/SteelBlue"
        android:minHeight="?attr/actionBarSize"
        android:theme="?attr/actionBarTheme"
        android:elevation="4dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <!-- Título centrado manteniendo los mismos márgenes exactos -->
        <TextView
            android:id="@+id/tvTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="104dp"
            android:layout_marginEnd="107dp"
            android:text="Registro de Usuario"
            android:textColor="@color/white"
            android:textSize="24sp"
            android:textStyle="bold"
            android:layout_gravity="center_horizontal"/>
    </androidx.appcompat.widget.Toolbar>

    <!-- Campos de texto con los mismos tamaños exactos pero mejor estilo -->
    <EditText
        android:id="@+id/etUsername"
        android:layout_width="404dp"
        android:layout_height="36dp"
        android:layout_marginTop="36dp"
        android:hint="Nombre de usuario"
        android:background="@drawable/edittext_background"
        android:padding="8dp"
        android:textColor="@color/black"
        android:textColorHint="@color/gray"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/toolbar2" />

    <EditText
        android:id="@+id/etPassword"
        android:layout_width="407dp"
        android:layout_height="37dp"
        android:layout_marginTop="16dp"
        android:hint="Contraseña"
        android:inputType="textPassword"
        android:background="@drawable/edittext_background"
        android:padding="8dp"
        android:textColor="@color/black"
        android:textColorHint="@color/gray"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/etUsername" />

    <EditText
        android:id="@+id/etConfirmPassword"
        android:layout_width="408dp"
        android:layout_height="39dp"
        android:layout_marginTop="16dp"
        android:hint="Confirmar contraseña"
        android:inputType="textPassword"
        android:background="@drawable/edittext_background"
        android:padding="8dp"
        android:textColor="@color/black"
        android:textColorHint="@color/gray"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/etPassword" />

    <!-- Botón con los mismos parámetros exactos pero mejor estilo -->
    <Button
        android:id="@+id/btnRegister"
        android:layout_width="410dp"
        android:layout_height="60dp"
        android:layout_marginTop="24dp"
        android:text="Registrarse"
        android:backgroundTint="@color/SteelBlue"
        android:textColor="@color/white"
        android:textSize="18sp"
        android:stateListAnimator="@null"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/etConfirmPassword" />
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="?attr/colorOnBackground"
        android:text=""/>


</androidx.constraintlayout.widget.ConstraintLayout>